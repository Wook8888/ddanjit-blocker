# PC · 맥북 차단 목록 동기화

딴짓 차단기는 **크롬 동기화**로 차단 목록을 기기끼리 자동으로 맞춥니다.
별도 서버나 로그인 창 없이, 크롬에 로그인한 구글 계정으로 구글이 대신 옮겨 줍니다.

| 동기화됨 | 기기마다 따로 |
|---|---|
| 차단 목록 · 예외 목록 | 차단 켜기/끄기 스위치 |
| 차단 화면 문구 · 이모지 | 사이트 접근 권한 (기기마다 한 번 허용) |
| 잠금 문제 단어 | 잠금 켜기/끄기 · 잠금 해제 상태 (5분) |
| 동기화 기록 (최근 10개) | 기기 이름 |

---

## 웹스토어에서 설치한 경우

**할 일 없음.** 두 기기 크롬에 같은 구글 계정으로 로그인하고 동기화가 켜져 있으면 됩니다.

크롬 **설정 → 나와 Google → 동기화 및 Google 서비스 → 동기화할 항목 관리** 에서
**확장 프로그램**이 켜져 있는지 확인하세요.

---

## 압축해제(개발자 모드)로 설치한 경우

압축해제로 설치하면 **폴더 경로에 따라 확장 ID가 달라져서** PC와 맥북을 서로 다른 확장으로 봅니다.
`key` 를 넣어 두 기기의 ID를 똑같이 고정해야 동기화됩니다.

1. [웹스토어 개발자 대시보드](https://chrome.google.com/webstore/devconsole) → **딴짓 차단기** → 왼쪽 **패키지**
2. **공개 키 보기** → `-----BEGIN PUBLIC KEY-----` 와 `-----END PUBLIC KEY-----` **사이의 문자열만** 복사 (줄바꿈은 지워서 한 줄로)
3. `extension/manifest.json` 위쪽에 한 줄 추가

   ```json
   {
     "manifest_version": 3,
     "key": "MIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8AMIIBCgKCAQEA...(복사한 문자열)",
     "name": "딴짓 차단기",
   ```

4. **PC와 맥북 둘 다** `chrome://extensions` 에서 딴짓 차단기를 **삭제** → **압축해제된 확장 프로그램을 로드합니다** 로 다시 로드
5. 두 기기 카드에 보이는 **ID가 같은지** 확인 (대시보드 상단의 항목 ID와도 같아야 합니다)

> 삭제하면 그 기기의 목록도 지워집니다. 불안하면 먼저 팝업 → **백업 · 불러오기 → 엑셀로 내보내기** 로 저장해 두세요.

---

## 처음 연결할 때

- 처음 연결한 뒤 **10분 동안은 두 기기 목록을 합칩니다.** 새 기기가 아직 다른 기기 목록을 받기 전이라도, 어느 쪽 목록도 사라지지 않게 하기 위해서입니다.
- 그 뒤부터는 한쪽에서 추가·삭제하면 다른 쪽에도 그대로 반영됩니다. 보통 몇 초에서 몇 분 걸립니다.
- 차단 화면 문구와 잠금 문제 단어는 **먼저 동기화를 시작한 기기의 것**을 따릅니다.

## 다른 기기에서 넘어온 사이트는 권한을 한 번 허용해야 합니다

사이트 접근 권한은 크롬이 기기마다 따로 관리해서, 동기화로 넘어오지 않습니다.
다른 기기에서 추가한 사이트가 넘어오면 툴바 아이콘에 노란 **!** 가 뜹니다.
팝업을 열고 **권한 한 번에 허용**을 누르면 그 기기에서도 차단이 시작됩니다.

## 용량

크롬 동기화는 확장 하나당 **100KB**, 항목 하나당 **8KB**까지입니다.
차단 목록과 기록 10개를 함께 담아야 해서, 도메인이 **300개**쯤 되면 오래된 기록부터 버립니다.
**350개**를 넘어서면 기록을 아예 남기지 못하고, 그때는 기록 화면에서 알려줍니다. 차단 자체는 그대로 동작합니다.

## 어느 기기가 바꿨는지 보기

팝업 맨 아래 줄에 **"🔄 맥북에서 변경 · 3분 전"** 처럼 마지막으로 목록을 바꾼 기기가 뜹니다.
누르면 최근 10개 기록이 열리고, 줄을 펼치면 **그때 목록이 통째로** 보입니다.
원하지 않는 변경이 있으면 그 자리에서 **이 상태로 되돌리기**를 누르면 됩니다 — 되돌린 결과도 모든 기기에 퍼집니다.

기기 이름은 처음엔 "Windows PC", "Mac" 처럼 자동으로 붙습니다.
팝업 **설정 → 기기 이름**에서 "회사 PC" 같은 이름으로 바꿔두면 기록이 훨씬 읽기 쉽습니다.
넘으면 팝업 아래에 안내가 뜨고, 목록은 그 기기에만 저장됩니다(차단은 정상 동작).

## 웹스토어에 올릴 때

- **업로드용 ZIP 에는 `key` 를 넣지 않습니다.** 스토어는 항목 ID를 이미 알고 있습니다. ZIP은 `key` 를 빼고 만들어 드립니다.
- 목록이 구글 동기화 서버를 거치게 되므로 **개인정보처리방침**과 웹스토어 **개인정보 보호 탭** 문구를 고쳐야 합니다. 제출할 때 같이 정리합니다.
