# 웹스토어 제출 체크리스트 (v2.2.0)

대시보드: https://chrome.google.com/webstore/devconsole → 딴짓 차단기

위에서부터 순서대로 하면 됩니다. 각 칸에 뭘 넣는지는 오른쪽 파일에 있습니다.

## 0. 먼저 — 깃허브에 올리기

v2.2.0 본체와 개인정보처리방침은 이미 올라가 있어요 (커밋 `f9c9d03`, 방침 페이지 반영 확인).
남은 건 **개발용 key 가 들어간 manifest.json**, **make-store-zip.py**, 이 체크리스트예요.

```
cd /d D:\Claude\ddanjit-blocker
git add .
git commit -m "manifest key 추가(기기 간 ID 고정), 스토어 zip 도구"
git push
```

## 1-0. 스토어용 zip 만들기

```
python make-store-zip.py
```

→ `ddanjit-blocker-v2.2.0.zip` (key 가 빠진 zip). 이걸 올립니다.

## 1. 패키지 탭

| 할 일 | 파일 |
|---|---|
| **새 패키지 업로드** | `ddanjit-blocker-v2.2.0.zip` |

> `extension/manifest.json` 에는 개발용 `"key"` 가 들어 있어요 (폴더에서 설치해도 스토어와 같은 ID가 되게 해서 기기 간 동기화 시험용).
> 스토어용 zip 은 **`python make-store-zip.py`** 로 만들면 `key` 가 자동으로 빠져요. `extension` 폴더를 직접 압축해서 올리지 마세요.

## 2. 스토어 등록정보 탭

| 칸 | 넣을 것 |
|---|---|
| 설명 | `listing.md` 3번 상자 전체 — **2.2.0 에서 문구가 바뀌었으니 새로 붙여넣기** |
| 카테고리 | Productivity → Workflow & Planning |
| 언어 | 한국어 |
| 스토어 아이콘 128×128 | `extension/icons/icon128.png` |
| 스크린샷 (기존 것 전부 지우고) | `screenshot_1.png` ~ `screenshot_5.png` 순서대로 — **2.2.0 화면으로 다시 만들었으니 새로 올리기** |
| 작은 프로모션 타일 440×280 | `promo_440x280.png` |
| 마퀴 프로모션 타일 1400×560 | `marquee_1400x560.png` (선택) |
| 홈페이지 URL | `https://github.com/Wook8888/ddanjit-blocker` |
| 지원 URL | `https://github.com/Wook8888/ddanjit-blocker/issues` |

스크린샷 순서: 1 기기 간 동기화(동기화된 기기 화면) → 2 차단 목록 → 3 차단 화면 → 4 예외 목록 → 5 단어 문제 잠금

## 3. 개인정보 보호 탭

| 칸 | 넣을 것 |
|---|---|
| `declarativeNetRequestWithHostAccess` 사유 | `submission.md` 해당 상자 |
| 단일 목적 · `storage` 사유 | `submission.md` — **2.2.0 에서 문구가 바뀌었으니 둘 다 새로 붙여넣기** |
| 원격 코드 | 아니요 |
| 데이터 수집 | **전부 체크 안 함** (인증 정보도 체크 안 함 — 비밀번호 기능 없음) |
| 인증 3개 | 모두 체크 |
| 개인정보처리방침 URL | `https://wook8888.github.io/ddanjit-blocker/` |

## 4. 계정 · 거래자

| 할 일 | 권장 |
|---|---|
| 거래자 여부 | **비거래자(Non-Trader)** — 무료 개인 프로젝트라 주소 서류가 필요 없음 |
| 연락처 이메일 | 계정 탭에서 인증 완료 여부 확인 |

## 5. 제출

오른쪽 위 **검토를 위해 제출**. 처음엔 공개 범위를 **비공개**로 두고 본인만 써본 뒤 공개로 바꿔도 됩니다.
심사는 보통 며칠, 길면 2~3주. 결과는 메일로 옵니다.
